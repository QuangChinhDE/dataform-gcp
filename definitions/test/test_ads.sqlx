config {
  type: "assertion",
  schema: "your_schema",
  name: "assert_ads"
}

select
  count(*) as invalid_count
from (
  select ad_id, count(*) 
  from ${ref('view_ads')}
  group by ad_id
  having count(*) > 1
) as duplicates