config {
  type: "assertion",
  schema: "your_schema",
  name: "assert_adsets"
}

select
  count(*) as invalid_count
from (
  select adset_id, count(*) 
  from ${ref('view_adsets')}
  group by adset_id
  having count(*) > 1
) as duplicates