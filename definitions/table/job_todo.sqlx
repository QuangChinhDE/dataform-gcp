config {
  type: "incremental",
  uniqueKey: ['job_todo_id'],
  schema: "customer_onboarding",
  bigquery: {
    partitionBy: "DATE(last_update)"
  }
}

SELECT DISTINCT * FROM ${ref('raw_job_todo')}