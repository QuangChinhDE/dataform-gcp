config {
  type: "incremental",
  uniqueKey: ["office_id"],
  schema: "human_resource"
}

SELECT DISTINCT * EXCEPT(row_number)
FROM (
SELECT *,
    ROW_NUMBER() OVER (PARTITION BY office_id ORDER BY last_update DESC) AS row_number
FROM ${ref('raw_office')}
) t
WHERE t.row_number = 1