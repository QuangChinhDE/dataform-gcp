config {
  type: "incremental",
  uniqueKey: ["area_id"],
  schema: "human_resource"
}

SELECT DISTINCT * EXCEPT(row_number)
FROM (
SELECT *,
    ROW_NUMBER() OVER (PARTITION BY area_id ORDER BY last_update DESC) AS row_number
FROM ${ref('raw_area')}
) t
WHERE t.row_number = 1