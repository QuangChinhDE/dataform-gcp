config {
  type: "incremental",
  uniqueKey: ['job_id_ban_giao'],
  schema: "customer_onboarding"
}

-- SELECT *, 1 as data_old FROM ${ref('raw_trien_khai_1_thang_ggsheet')}
-- UNION ALL
SELECT *, 0 as data_old FROM ${ref('raw_job_cohn_onboarding_month')}
