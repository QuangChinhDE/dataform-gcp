config {
  type: "table",
  schema: "marketing"
}

SELECT DISTINCT
    marketer
    ,code
    ,SAFE_CAST(account_id AS INT64) account_id
    ,status
    ,start_date
    ,update_at
    ,source_id
FROM ${ref('raw_marketer')}
WHERE SAFE_CAST(account_id AS INT64) IS NOT NULL